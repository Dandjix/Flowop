using UnityEngine;

public class SurfaceContext : MonoBehaviour
{
    public enum SurfaceCharacteristics
    {
        Rocks,
        Grass
    }

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
